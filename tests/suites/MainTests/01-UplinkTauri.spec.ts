describe("Tauri simple test", async () => {
  it("Initial test in tauri", async () => {
    await $("#unlock-layout").waitForDisplayed();
    /*await $("aria/pin-input").setValue("1234");
    await $("aria/create-account-button").click();
    await $("aria/unlock-layout").waitForDisplayed();
    await $("aria/username-input").setValue("ChatUserA");
    await $("aria/create-account-button").click();
    await $("aria/welcome-screen").waitForDisplayed();*/
  });
});
